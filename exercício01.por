programa {
  funcao inicio() {
    inteiro dividendo, divisor, resto

    dividendo = 25
    divisor = 3
    resto = dividendo / divisor
    escreva("O resto da divisão de " + dividendo + " por " + divisor + " é: " + resto)
  }
}